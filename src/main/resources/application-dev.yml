server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://111.230.23.59:3306/luc_bs?serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&useUnicode=true&useSSL=false
    username: root
    password: lsl-123456

  thymeleaf:
    prefix: classpath:/templates/
  mvc:
    favicon:
      enabled: false
mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.edu.luc_bs.Entity

pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql
